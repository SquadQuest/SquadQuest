<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>

	<key>teamID</key>
	<string>64TVJXTLTT</string>

	<!--
	  Automatic signing, not manual: Runner embeds the ImageNotification
	  extension, so a manual export would need a named profile per bundle id
	  (app.squadquest AND app.squadquest.ImageNotification) stored as secrets
	  and kept in sync here. The archive/export steps instead pass
	  -allowProvisioningUpdates with an App Store Connect API key, which
	  resolves a profile for every target with nothing pinned in the repo.
	-->
	<key>signingStyle</key>
	<string>automatic</string>

	<key>uploadSymbols</key>
	<true/>

	<!-- Write the .ipa to disk; the workflow uploads it as a separate step. -->
	<key>destination</key>
	<string>export</string>
</dict>
</plist>
