# SquadQuest V2 Specs — moved

The v2 design now lives as structured, spec-driven docs in **[`/specs`](../../specs/)**
(SpecOps): the source of truth that the implementation is brought into conformance with.

Start here:

- **[`specs/README.md`](../../specs/README.md)** — how the specs work + layout
- **[`specs/principles.md`](../../specs/principles.md)** — the philosophy, written down
- **[`specs/architecture.md`](../../specs/architecture.md)** — stack, backend topology, v1→v2 transition
- **[`specs/data-model.md`](../../specs/data-model.md)** — the v2 schema
- **[`specs/api/`](../../specs/api/)** — the versioned client↔server contract
- **[`specs/screens/`](../../specs/screens/)** & **[`specs/behaviors/`](../../specs/behaviors/)** — the v2 experience

Work-in-flight is tracked in **[`/plans`](../../plans/)**.

> This narrative doc was the original v2 spec; its content has been ported into `specs/` so
> there is a single source of truth. This pointer is kept to avoid dangling links.
