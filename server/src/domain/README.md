# domain/

The one current domain model + business logic. Speaks a single, current shape —
versioning lives at the route edge (`../contracts/`), never here. See
`specs/architecture.md` and `specs/data-model.md`.
